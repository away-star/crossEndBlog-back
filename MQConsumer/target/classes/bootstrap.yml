spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ce_content?useSSL=false&serverTimezone=GMT%2B8&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    password: "030321"
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: hikariCP
      minimum-idle: 20
      maximum-pool-size: 50
      auto-commit: true
      idle-timeout: 10000
      max-lifetime: 30000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  application:
    name: MQConsumer
  cloud:
    config:
      #设置本地优先级最高
      allow-override: true
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: 2d5c46e8-08d2-4ea8-94c9-d375279cac7b
        group: crossEndBlog
      config:
        namespace: 2d5c46e8-08d2-4ea8-94c9-d375279cac7b
        #group为public情况下可以省略
        group: crossEndBlog
        file-extension: yaml
        #是否支持动态刷新
        refresh-enabled: true
        shared-configs:
          - data-id: common.yaml
            #group为public情况下可以省略
            group: crossEndBlog
            #是否支持动态刷新
            refresh: true
          - data-id: MQ.yaml
            group: crossEndBlog
            refresh: true
  mail:
    # SMTP服务器
    host: smtp.163.com
    # 发送验证码邮箱
    username: q2064989403@163.com
    # 授权码
    password: HTQQVIBEFOEPGUNS
    # 编码
    default-encoding: utf-8
    port: 465
    protocol: smtps
  rabbitmq:
    port: 9204
    host: localhost
    username: cross
    password: cross

  #手动指定开发环境
  profiles:
    active: dev

server:
  port: 9112

